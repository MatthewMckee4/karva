coverage:
  status:
    project:
      default: # default is the status check's name, not default settings
        target: auto
        threshold: 5
    patch:
      default:
        enabled: false

codecov:
  require_ci_to_pass: false
  token: not_required
