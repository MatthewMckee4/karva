[project]
name = "example"
version = "0.1.0"
description = "Example project with tests using Karva"
readme = "README.md"
requires-python = ">=3.9"
dependencies = ["pytest"]

[tool.ruff.lint]
select = ["ALL"]
ignore = ["D", "INP001", "PLR2004", "S101"]
